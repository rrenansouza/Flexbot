<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Botão Glow – Paleta Âmbar</title>
<style>
  /* =========================
     🎨 Paleta (inspirada na imagem)
     ========================= */
  :root{
    --amber: #EDAE29;   /* base do botão */
    --amber-600:#E89617;/* hover: um pouco mais laranja */
    --amber-400:#F6C24A;/* hover: dourado claro */
    --amber-300:#FAD46F;/* brilho suave */
    --amber-700:#D87C0A;/* active: mais escuro */
    --text: #0e0e0e;    /* texto escuro como no seu botão */
    --bg:   #f5f5f5;    /* só pra página de demo */
  }

  /* layout de demo */
  body{
    margin:0; height:100dvh; display:grid; place-items:center;
    background:var(--bg); font-family:system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,Arial;
  }

  /* =========================
     Botão Glow (hover + clique)
     ========================= */
  .glow-btn{
    --h: 54px;
    position:relative; isolation:isolate;
    display:inline-flex; align-items:center; justify-content:center;
    height:var(--h); padding:0 28px;
    border-radius:999px;                 /* formato “pill” como na imagem */
    border:2px solid color-mix(in oklab, var(--amber), black 10%);
    background:var(--amber);
    color:var(--text);
    font-weight:700; letter-spacing:.2px;
    cursor:pointer; user-select:none; -webkit-tap-highlight-color:transparent;
    transition:
      transform .2s cubic-bezier(.2,.8,.2,1),
      box-shadow .25s ease, border-color .25s ease,
      background .25s ease, color .25s ease;
    box-shadow:
      0 2px 0 rgba(0,0,0,.08),
      0 10px 24px rgba(237,174,41,.25);
    outline:0;
  }

  /* halo animado */
  .glow-btn::before{
    content:"";
    position:absolute; inset:-60%;
    background:conic-gradient(
      from 0deg,
      var(--amber-400),
      var(--amber-600),
      var(--amber-300),
      var(--amber-400)
    );
    filter:blur(24px);
    opacity:0; transition:opacity .25s ease;
    z-index:-1;
    animation:spin 7s linear infinite;
  }

  /* brilho interno sutil (fica mais visível no hover) */
  .glow-btn::after{
    content:"";
    position:absolute; inset:2px;
    border-radius:inherit;
    box-shadow:inset 0 0 0 0 rgba(255,255,255,.0);
    transition:box-shadow .25s ease;
    pointer-events:none;
  }

  /* Hover: mais brilho e leve mudança de matiz */
  .glow-btn:hover{
    background:linear-gradient(90deg, var(--amber), var(--amber-400));
    border-color:color-mix(in oklab, var(--amber-600), black 10%);
    box-shadow:
      0 2px 0 rgba(0,0,0,.08),
      0 12px 28p
